{
  "name": "echos-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:blog": "node scripts/build-blog.js",
    "dev": "node scripts/build-blog.js && vite",
    "build": "node scripts/build-blog.js && vite build && node scripts/build-blog.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^17.0.3",
    "vite": "^7.3.1"
  }
}